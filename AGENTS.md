# AGENT 必知必会

## 归档命令 /roll-up

触发词：`归档`、`帮我归档`、`执行归档`、`archive`。

**文档结构**

```text
本次归档涉及 4 类文档，分布在 docs/、模块目录与根目录
        │
        ├─▶ 业务（面向 user）
        │     ├─▶ 历史：docs/note-*.md
        │     └─▶ 最新：README 分两层
        │           ├─▶ 根 README.md        （导航指路）
        │           └─▶ 模块 README.md      （模块详情，位于各模块目录内）
        │
        └─▶ AI（面向 AI）
              ├─▶ 历史：docs/note-agent.md
              └─▶ 最新：AGENTS.md
```

本次归档共涉及 4 类文档：业务历史/最新、AI 历史/最新。业务最新态分两层——根 README 与各模块目录内的 README。各自角色与写入格式在归档流程中说明。

**归档流程**

```text
归档触发：回顾对话，识别内容性质（决策/踩坑/权衡/选型）
   │
   ▼
判断归属
   ├─▶ [业务] 操作/用法类内容 —— 怎么用（面向 user）
   │     │
   │     ▼
   │  过程文档 docs/note-*.md ← 为什么这么定（历史）
   │     │
   │     ▼
   │  最终状态 模块 README.md ← 该模块怎么用（当前生效）
   │     │
   │     ▼
   │  最终状态 根目录 README.md ← 导航指路（涉及新模块时）
   │
   └─▶ [AI] 归档规范类内容 —— 按什么规矩（面向 AI）
         │
         ▼
      过程文档 docs/note-agent.md ← 为什么改（历史）
         │
         ▼
      最终状态 AGENTS.md ← 归档规范（当前生效）

图例：过程文档 = 决策/踩坑/权衡，历史；最终状态 = 当前生效、可直接执行
```

图中关键步骤：

- **回顾对话**：识别高价值内容——决策、踩坑、权衡、选型
- **判断归属**：按上图分流，处理链顺序固定、不可跳步——业务侧先追加 note-*、再更新模块 README、最后根 README 指路；AI 侧先追加 note-agent、再更新 AGENTS.md
- **去重**：先读目标文件已有内容，若已覆盖则跳过
- **写入**：新内容追加到目标文件**顶部**（不改动原文）
- **告知**：归档完成后说明写入的位置与内容

**各文档写入格式**

- **note-*.md（业务历史）**：`#` = 领域（O）；`##` = 关键里程碑（KR），标题直接陈述成果并附日期（如 `## 日期格式 260819`）；正文写关键认知；条目按对话先后（正向时间线）排列；先摆框架（对比表/链路图）再让结论从证据中浮现
- **README.md（业务最新，分两层）**：根 README 只做导航——两级标题，每节只放三种数据（图/流程导航标注命令编号、表/决策依据回答"选谁"、命令/可执行按编号分段）；模块 README 讲详情——最多三级标题（`###` 可用）、`##` 不带时间信息、可直接执行，只写当前生效内容
- **note-agent.md（AI 历史）**：格式同 note-*，记录归档规范每一步为什么这么改
- **AGENTS.md（AI 最新）**：当前生效的归档规范，AI 自动加载执行
- **通用规则**：角色定位——过程文档 = 为什么这么定（历史），最终态 = 当前生效、可直接执行；README 给 user 看怎么用，AGENTS 给 AI 看按什么规矩维护；信息不冗余——同一事实只在一处出现（根 README 只做导航、模块 README 讲详情，README 写当前生效、note 写历史）

**演化图画法**

- **形式必须匹配内容**：拓扑图（组件关系）画不了操作流程，操作导航图用岔路口流程
- **样式选型**（按对话逻辑选用，默认岔路口优先）：
  - **岔路口样式**：方案对比/认知分歧时用。`├─▶ [分支]` 列探索分支，`✖ 否决` 标注被推翻的选项，`└─▶ [采纳]` 指向最终选择
  - **时间线框式**：演进步骤清晰、每步有强痛点驱动时用。`┌─┐` 框住各阶段事件，侧边 `[顾虑爆点]` 标注卡点
  - **对话气泡式**：内容以"快问快答/打磨细节"为主时用。左列演化轨、右列用户原话一一对应
- **图文配对**：每张图下必须紧跟文字（说明/关键认知），文字只写图外增量、不重复图内已画内容；一组图文 = 一组相关问题/领域，说明不了就新建一组
- **大图拆分**：图超过约 30 行拆成多组图文（总分结构），不画"总图方块"导航
- **不以结论收尾**：结尾停在卡点/待解需求，方案由正文承载
- **原话佐证**：演化链每一环都要能在对话里找到用户原话或明确意图作证据；找证据按**时间顺序回看整段对话**，不是只看最近几条；找不到依据的环节 = AI 脑补，删掉重写；用户没明说但驱动决策的动机，从后续发言反推确认，而不是猜
- **检验标准**：演化图每一层都能在对话中找到出处；找不到的就是编的

**归档原则**

- **去重优先**：只归档有价值内容；被推翻的**方案细节**不记，但**权衡过程与卡点（为什么不行）**必须记——那才是真需求的来源
- 问题按提问顺序逐条列，不压缩成一段
- 不改原内容，新内容只追加顶部
- 涉及 AI 行为的归档到 `note-agent.md` 本身
