# AGENT 必知必会

## 归档命令 /roll-up

触发词：`归档`、`帮我归档`、`执行归档`、`archive`。

### 执行步骤

1. **回顾对话**：识别有价值内容——决策、踩坑、权衡、选型；**图与表优先保留**（整张对比表含维度，不删表只留结论）。
2. **判断归属**：
   - `README.md`：面向 user——操作步骤、使用方法。
   - `docs/note-*.md`：面向 builder ——决策理由、踩坑过程。
3. **去重**：先读目标文件已有内容，若已覆盖则跳过。
4. **写入**：新内容追加到目标文件**顶部**（不改动原文）。
5. **告知**：归档完成后说明写入的位置与内容。

### 文档结构（O/KR 式）

- `#` = 领域 = O（目标/方向）
- `##` = 关键里程碑 = 成果 = KR（可验证的成果）
- **标题直接陈述成果，并附日期**（如 `## 日期格式 260819`），正文写关键认知
- 文件内条目按对话先后（正向时间线）排列
- 先摆框架（对比表/链路图），再让结论从证据中浮现

### 演化过程写法：用图还原对话轨迹，不写结论

- **用 ASCII 竖向流程图**描述演化：每层客观陈述事实/需求/动机，箭头旁注转折点（`←`）
- **不用 `>` 块、不用"起念/警觉"式标签词**：图作正文第一块，直接陈述
- **不以结论收尾**：结尾停在卡点/待解需求，方案由正文承载
- **内容必须有对话原话佐证**：
  - 演化链每一环都要能在对话里找到用户原话或明确意图作证据
  - 找证据按**时间顺序回看整段对话**，不是只看最近几条
  - 找不到依据的环节 = AI 脑补，删掉重写
  - 用户没明说但驱动决策的动机，从后续发言反推确认，而不是猜
- **检验标准**：演化图每一层都能在对话中找到出处；找不到的就是编的

### 归档原则

- **去重优先**：只归档有价值内容；被推翻的**方案细节**不记，但**权衡过程与卡点（为什么不行）**必须记——那才是真需求的来源
- 问题按提问顺序逐条列，不压缩成一段
- 不改原内容，新内容只追加顶部
- 涉及 AI 行为的归档到 `note-agent.md` 本身
