# AGENT 必知必会

## 归档命令 /roll-up

触发词：`归档`、`帮我归档`、`执行归档`、`archive`。

**文档结构**

```text
本次归档涉及 4 类文档，全部集中在 docs/ 与根目录
        │
        ├─▶ 业务（面向 user）
        │     ├─▶ 区块·历史：docs/note-*.md（含 note-base.md 通用库）
        │     └─▶ 世界状态·最新
        │           ├─▶ docs/mem-*.md  （当前生效值，变更即覆盖）
        │           └─▶ 根 README.md   （导航指路）
        │
        └─▶ AI（面向 AI）
              ├─▶ 区块·历史：docs/note-agent.md
              └─▶ 世界状态·最新：AGENTS.md
```

本次归档共涉及 4 类文档：业务历史/最新、AI 历史/最新。docs/ 单轨管理——`note-*` 为区块（不可变历史，只追加不改），`mem-*`/AGENTS/根 README 为世界状态（当前现值，变更即覆盖）。各自角色与写入格式在归档流程中说明。

**归档流程**

```text
归档触发：回顾对话，识别内容性质（决策/踩坑/权衡/选型）
   │
   ▼
判断归属
   ├─▶ [业务] 操作/用法类内容 —— 怎么用（面向 user）
   │     │
   │     ▼
   │  归属判断：按主题分类，不按模块
   │     ├─▶ 跨模块通用（网络/代理/下载/镜像排障）
   │     │     └─▶ 区块 docs/note-base.md
   │     └─▶ 模块专属 ─────────────▶ 区块 docs/note-<模块>.md
   │                                    │
   │                                    ▼
   │                                 世界状态 docs/mem-<模块>.md ← 当前生效值
   │                                    │
   │                                    ▼
   │                                 世界状态 根 README.md ← 导航指路（涉及新模块时）
   │
   └─▶ [AI] 归档规范类内容 —— 按什么规矩（面向 AI）
         │
         ▼
      区块 docs/note-agent.md ← 为什么改（历史）
         │
         ▼
      世界状态 AGENTS.md ← 归档规范（当前生效）

图例：区块 = 决策/踩坑/权衡，不可变历史（只追加不改）；世界状态 = 当前生效、可直接执行（变更即覆盖）
```

图中关键步骤：

- **回顾对话**：识别高价值内容——决策、踩坑、权衡、选型
- **判断归属**：按上图分流，处理链顺序固定、不可跳步——业务侧先追加 `note-*`（按主题归 note-base 或对应模块）、再更新 `mem-*`、最后根 README 指路；AI 侧先追加 note-agent、再更新 AGENTS.md
- **去重**：先读目标文件已有内容，若已覆盖则跳过
- **写入**：新内容追加到目标文件**顶部**（不改动原文）
- **告知 + 询问**：每次只归档一个文件，完成后说明写入的位置与内容，并询问是否继续下一个；**不自动跑完整个处理链**——第一个落点文件做完即停，后续步骤等用户确认后再做

**各文档写入格式**

- **`note-*.md`（业务历史，区块）**：`#` = 领域（O）；`##` = 关键里程碑（KR），标题直接陈述成果并附日期（如 `## 日期格式 260819`）；正文写关键认知；条目按对话先后（正向时间线）排列；先摆框架（对比表/链路图）再让结论从证据中浮现；归属按主题——跨模块通用进 `note-base.md`，模块专属进对应 `note-<模块>.md`
- **`mem-*.md`（业务最新，世界状态）**：当前生效的关键值（版本、参数、地址、连接信息等），变更即覆盖旧值、不保留过程；给后续 Agent 接手时读这里拿现值，"为什么"回 `note-*` 查
- **根 README.md（业务最新，世界状态）**：只做导航指路——两级标题，每节只放三种数据（图/流程导航标注命令编号、表/决策依据回答"选谁"、命令/可执行按编号分段）
- **`note-agent.md`（AI 历史，区块）**：格式同 `note-*`，记录归档规范每一步为什么这么改
- **AGENTS.md（AI 最新，世界状态）**：当前生效的归档规范，AI 自动加载执行
- **通用规则**：角色定位——区块 = 为什么这么定（不可变历史，只追加不改），世界状态 = 当前生效、可直接执行（变更即覆盖）；README/mem 给 user 看怎么用，AGENTS 给 AI 看按什么规矩维护；信息不冗余——同一事实只在一处出现（"现在是什么"进世界状态，"为什么变成这样"进区块）

**演化图画法**

- **形式必须匹配内容**：拓扑图（组件关系）画不了操作流程，操作导航图用岔路口流程
- **样式选型**（按对话逻辑选用，默认岔路口优先）：
  - **岔路口样式**：方案对比/认知分歧时用。`├─▶ [分支]` 列探索分支，`✖ 否决` 标注被推翻的选项，`└─▶ [采纳]` 指向最终选择
  - **时间线框式**：演进步骤清晰、每步有强痛点驱动时用。`┌─┐` 框住各阶段事件，侧边 `[顾虑爆点]` 标注卡点
  - **对话气泡式**：内容以"快问快答/打磨细节"为主时用。左列演化轨、右列用户原话一一对应
- **图文配对**：每张图下必须紧跟文字（说明/关键认知），文字只写图外增量、不重复图内已画内容；一组图文 = 一组相关问题/领域，说明不了就新建一组
- **大图拆分**：图超过约 30 行拆成多组图文（总分结构），不画"总图方块"导航
- **不以结论收尾**：结尾停在卡点/待解需求，方案由正文承载
- **原话佐证**：演化链每一环都要能在对话里找到用户原话或明确意图作证据；找证据按**时间顺序回看整段对话**，不是只看最近几条；找不到依据的环节 = AI 脑补，删掉重写；用户没明说但驱动决策的动机，从后续发言反推确认，而不是猜
- **检验标准**：演化图每一层都能在对话中找到出处；找不到的就是编的

**归档原则**

- **去重优先**：只归档有价值内容；被推翻的**方案细节**不记，但**权衡过程与卡点（为什么不行）**必须记——那才是真需求的来源
- **世界状态与区块分离**：待办/进展/卡点 = 未完成的历史，进 `note-*`（区块）；当前生效值进 `mem-*`/README/AGENTS（世界状态）；mem 不存历史，note 不存现值
- 问题按提问顺序逐条列，不压缩成一段
- 不改原内容，新内容只追加顶部
- 涉及 AI 行为的归档到 `note-agent.md` 本身
